version: '3'
services:
  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "888:80"
    volumes:
      - "./nginx:/etc/nginx/conf.d"
      - "..:/var/www/html"
    links:
      - php
  php:
    build: php
    volumes:
      - "..:/var/www/html"
